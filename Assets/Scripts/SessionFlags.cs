public static class SessionFlags
{
    // Se pone en true al pulsar Play
    public static bool cameFromPlay = false;

    // Para que el vídeo+salto al nivel 1 ocurra SOLO una vez por ejecución
    public static bool introAlreadyDone = false;
}